# --- Segredos: nunca versionar ---
.env
.env.*
*.key
*.pem
segredos.*
# --- Config de MCP: contém a API Key em texto puro ---
# O `claude mcp add --scope project` grava o header Authorization aqui.
# Versionar isto publicaria a credencial do Shift.
.mcp.json

**/credenciais*
.claude/settings.local.json

# --- Sistema operacional ---
Thumbs.db
desktop.ini
.DS_Store

# --- Temporários / editores ---
*.tmp
*.bak
*~
.vscode/
.idea/

# --- Exports brutos do Shift que forem descartáveis ---
# (fluxos/**/fluxo.yaml é versionado de propósito — ver docs/convencoes.md)
*.log
